import { DragDropListDirective } from './drag-drop-list.directive';

describe('DragDropListDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropListDirective(null);
    expect(directive).toBeTruthy();
  });
});
